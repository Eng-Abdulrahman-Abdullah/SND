this is SND Risk System ( A smart model, Working on analyze User Behavior and initial A uniq baseline for each user And compare them with the real time features ) 
for local Run ( Python run.py ) 
the dashboard interface ( http://127.0.0.1:5000/dashboard ) 
the health check interface ( http://127.0.0.1:5000/health ) 
